# AI Capstone 2026 春季 — 專題報告（進階組）

**團隊：** Team 5
**基礎任務：** 玩具積木收集（客廳場景）
**進階任務：** 移動方塊抓取 — 「捕捉移動的老鼠」（`HCIS-MovingCubeGrasp-SingleArm-v0`）

---

## 1. 任務說明

### 1.1 任務概述

我們將基礎級的**玩具積木收集**任務延伸為動態目標變體，命名為**移動方塊抓取**（內部暱稱「捕捉移動的老鼠」）。場景沿用基礎級的客廳配置（`LIVING_ROOM_CFG`）與 Franka Panda 機械臂，但將原本的三個靜態玩具積木替換為一個 5 cm、0.05 kg 的藍色方塊，該方塊在整個回合期間以**固定速度在桌面上滑動**。機器人必須攔截移動中的方塊、抓取後將其搬運至 Hello Kitty 置物籃（`basket_23`），並將其放入籃中。

![任務場景佔位圖](report_fig/PLACEHOLDER_advanced_scene.png)

*圖 1.（佔位圖）模擬場景 — 移動的藍色方塊與目標置物籃。請插入 Isaac Sim 實際rollout/錄影截圖。*

與基礎任務不同的是，基礎任務中積木位置在回合開始後便固定不動；而本任務中，方塊的位置每個時步都在改變。這意味著策略無法單純記憶目標位置，而必須從 RGB 影像中持續感知方塊的移動，並及時反應以在方塊滑出可達範圍前完成攔截。

### 1.2 目標與成功標準

當方塊最終落入置物籃內，即視為**成功**——正式定義為：方塊與籃子中心的水平距離低於 0.10 m，且方塊相對籃子的高度落在籃子內部的 z 範圍內（`cube_in_basket`）。每個回合的時間上限為 30 秒（方塊在回合開始時即立即移動，給機械臂有限的攔截時間窗口）。

我們追蹤三項指標，依照評估腳本 `eval/moving_cube_grasp_eval.py` 定義：

- **捕捉率**：機械臂成功抓取並舉起方塊的回合比例。
- **放置率**：在**已捕捉**的方塊中，成功放入置物籃的比例。
- **端對端成功率**：完成完整「捕捉 → 放置」流程（達到 `success` 終止條件）的回合比例。

我們在提案時設定的目標為：**捕捉率 ≥ 60%**、**放置率 ≥ 80%**，並在三種方塊速度（0.08、0.12、0.18 m/s）下測試泛化能力。

### 1.3（進階）提出新任務的動機

我們的提案以一個現實的公共衛生問題為出發點：台北部分地區反覆發生鼠患（老鼠/大鼠出沒於公共場所與住宅空間），難以徒手快速捕捉。這啟發了本任務的標語：**「用 Physical AI 捕捉移動的老鼠！」**——以機械臂攔截並移除小型、快速、移動路徑難以預測的目標，作為自動化害蟲捕捉系統的簡化代理問題。

為了在模擬中研究此問題，我們以滑動的方塊取代「老鼠」，以便在不需要建模動物行為複雜性的前提下，專注於感知與控制的核心問題。基礎級的取放任務只需要策略定位**靜態**物體；我們希望將專題推進到更接近現實操作場景的能力：處理一個**位置持續改變且策略無法預知**的目標。這在基礎流程之上額外引入了四項新需求：

1. **感知**：僅從 RGB 相機影像推斷方塊的運動（方向與速度），因為策略在推論時無法取得方塊的真實狀態。
2. **運動規劃**：規劃攔截軌跡，在方塊滑出工作空間之前抵達目標位置，而非單純朝方塊的當前位置移動。
3. **在運動不確定性下抓取**：在目標仍在移動（或剛因摩擦力停止）的情況下執行穩定抓取，此時微小的時機誤差會導致夾爪夾到空氣或夾住方塊邊緣。
4. **完整流程銜接**：將攔截、抓取、搬運、放置串聯為單一連續回合，並遵守與基礎任務相同的模仿學習限制（FSM 生成示範、策略僅以 RGB 影像 + 關節狀態訓練）。

為使合成資料生成具有可行性，我們設計了一個**具有特權存取的 FSM 專家**，可取得方塊與置物籃的真實位姿與速度。此專家在 Isaac Sim 中生成示範；僅保留成功回合。相對地，訓練好的策略在任何時候都看不到這些特權資訊——它完全依賴兩個機載相機（腕部 + 正面）和本體感知的關節狀態，與基礎級的觀測設計完全一致。本任務的所有 USD 資產（方塊、置物籃）均獨立於基礎級場景資產單獨配置，符合進階提交要求。

---

## 2. 資料收集

### 2.2 在 Isaac Sim 中的模擬資料收集

#### 2.2.1 資料集組成與規模

我們分兩個階段生成訓練資料，對應方塊運動模型的兩次迭代（詳見 §2.2.4 與 §5 說明模型修改的原因）。

**第一階段資料集**（`mikehsuhoodie/moving-cube-grasp-fsm-20260607-mike`）：從 80 個手動選定的初始方塊位置（`data/moving_cube_demo/object_poses.json`，共 80 筆）生成。FSM 專家對每個位置執行一次，僅保留成功回合——**80 次嘗試中有 78 次成功（97.5%）**，共生成跨 78 個回合的 **48,750 幀**（平均每回合約 625 幀，約 20.8 秒）。此資料集用於訓練第一個 Diffusion Policy 檢查點（見 §3）。

**第二階段資料集**（`yun0523/moving-cube-grasp`）：在重新設計方塊速度採樣分佈（§2.2.4）並改用 200 個均勻採樣初始位置（`data/moving_cube_demo/object_poses_200.json`，x ∈ [0.10, 0.52]，y ∈ [-0.65, -0.15]，排除距置物籃 0.18 m 以內的位置）後生成。FSM 專家同樣僅保留成功回合——**200 次嘗試中有 195 次成功（97.5%）**。這 195 個回合後續在訓練 SmolVLA 策略（§3）時以 **175 / 20** 切分為訓練集 / 驗證集。

#### 2.2.2 資料收集方法細節

進階任務的所有資料均為**在 Isaac Sim 中合成生成**，因為動態目標設定無法使用基礎任務的 UMI 手持設備進行實物採集。每個回合的生成流程如下：

1. **生成**：將方塊放置於預先採樣的初始位置之一（第一階段：80 個靠近工作空間中心的位置；第二階段：200 個分布於完整工作空間的位置）。
2. **速度採樣**：在回合開始時採樣一次固定水平速度 `(vx, vy)`（方向與大小的規則在兩個階段間不同——見 §2.2.4），並透過 `write_root_velocity_to_sim` 在每個時步克服摩擦力，使方塊以恆定速度沿直線滑動。
3. **FSM 專家 rollout（7 個階段，60 Hz）**——特權專家（從基礎級的 `ToyBlocksCollectionStateMachine` 延伸為單物體、移動目標變體）執行：
   - 階段 0 — *懸停*：從靜止姿態移至方塊初始位置上方。
   - 階段 1 — *接近*：在持續追蹤方塊運動的同時下降（第二階段新增預測性「前導項」，見 §2.2.4）。
   - 階段 2 — *抓取*：閉合夾爪。
   - 階段 3 — *提起*：舉起方塊。
   - 階段 4 — *搬運*：夾持方塊移至置物籃上方。
   - 階段 5 — *下降*：進入置物籃。
   - 階段 6 — *放開*：張開夾爪並後退。

   在後期迭代版本中，方塊在被提離桌面之前持續接受速度注入，因此 FSM 必須追蹤一個在階段 0–2 仍在移動的目標，而非一個方便地在被接近時靜止不動的目標。
4. **錄製**：腕部相機 RGB（640×480）、正面相機 RGB（640×480）、關節位置/速度（絕對值與相對於預設姿態的相對值）、上一動作，以及關節位置目標，均以 LeRobot 資料集格式錄製。未達 `cube_in_basket` 成功條件的回合一律丟棄（`EXPORT_SUCCEEDED_ONLY`）。

#### 2.2.3 資料視覺化

*（佔位圖——請在此插入腕部/正面相機範例幀，以及來自 `object_poses.json` / `object_poses_200.json` 的方塊軌跡圖。）*

#### 2.2.4（進階）與基礎級的差異與設計說明

| 面向 | 基礎（玩具積木收集） | 進階 — 第一階段 | 進階 — 第二階段 |
|---|---|---|---|
| 物體 | 3 個積木 + 1 個收納盒 | 1 個滑動方塊（5 cm，0.05 kg）+ Hello Kitty 置物籃 | 同上 |
| 物體狀態 | 靜態 | 以固定速度移動，**方向偏向工作空間中心**（從生成位置到中心的方位角 ± 60° 抖動） | 以固定速度移動，**方向與生成位置完全解耦**（在 [0, 2π] 中均勻隨機抽取角度，以不超出桌面為條件進行拒絕採樣） |
| 攔截策略 | 不適用 | FSM 追蹤方塊的*當前*位置（無前導項） | FSM 新增**預測性前導項**：`target_xy += vel_xy × remaining_steps × dt × 0.2` |
| 抓取高度偏移 | `_GRASP_Z_OFFSET = 0.02`（基礎預設值） | `+0.08`（升高以避免壓入桌面） | `+0.04`（降低——第一階段偏移量過高，導致難以可靠抓住方塊） |
| FSM 階段 | 多物體 7 階段取放，重複 ×3 | 單物體 7 階段攔截 + 預測前導 + 放置 | 相同結構，調整各階段時序 |
| 終止條件 | 全部 3 個積木進入盒內 | `cube_in_basket`（xy 半徑 < 0.10 m，z 在籃子內部範圍內） | 同上，另新增 `cube_fallen` 終止條件（方塊滑落桌面） |
| 回合長度 | 15 秒（預設） | 30 秒 | 30 秒 |
| 資料來源 | 真實 UMI 採集 + 模擬 | 純合成（`object_poses.json`，80 個位置） | 純合成（`object_poses_200.json`，200 個位置，均勻分布於工作空間） |
| 新增配置參數 | — | `cube_linear_speed_range`、`cube_workspace_x/y`、`basket_pos` | 同上，速度方向範圍擴大（±90°） |

---

## 3. 策略訓練

### 3.1 模型架構

#### 3.1.1 基線架構

我們的基線策略與基礎任務相同，使用 **Diffusion Policy**（LeRobot `policy.type=diffusion`），透過 `cmd/train_moving_cube.sh` 以 LeRobot 預設超參數訓練。觀測空間與基礎任務一致：兩個 RGB 相機（`wrist`、`front`，640×480）加上本體感知關節狀態（`joint_pos`、`joint_vel`、`joint_pos_rel`、`joint_vel_rel`、`last_action`、`joint_pos_target`）。動作空間為 LeRobot 格式的機械臂末端執行器軌跡。在任何時刻，策略均無法取得方塊的真實狀態。

#### 3.1.2（進階）架構修改與動機

在第一階段 Diffusion Policy 展現出有前景但分布過窄的結果（§4）後，我們針對第二階段的泛化目標嘗試了另外兩種架構：

- **ACT（`policy.type=act`，`chunk_size=16`，`n_action_steps=16`）**：選用原因為基礎任務中 ACT chunk-16 已達到約 90% 的成功率，且 ACT 的動作分塊機制適合操作任務。然而，我們發現 LeRobot 原版 ACT 實作從**單幀當前觀測**預測動作序列，沒有內建機制消費多幀近期影格。對於移動目標，這意味著策略可以看到*方塊現在在哪*，卻無法得知*它往哪個方向移動或移動多快*。要給 ACT 真正的多幀時序上下文，需要修改策略原始碼、觀測流程、訓練批次維度和推論歷史緩衝——在剩餘時程下判斷成本過高。因此我們按現有狀態訓練並評估了 ACT（§4），但未進一步推進。
- **SmolVLA 加時序視覺輸入（`policy.type=smolvla`，`chunk_size=16`，`n_action_steps=4`）**：選用原因為這是一種工程成本較低的多幀上下文提供方式：每個訓練樣本由 `front` 和 `wrist` 相機**最近 4 幀**構成（共 8 張影像：`front_t-3..t0`、`wrist_t-3..t0`），透過自訂的時序資料集包裝器實現（全域索引 → 回合/幀映射、回合內歷史補齊使用首幀填充、回合內動作分塊目標使用末動作填充）。`n_action_steps=4` 的設定是專門針對*動態*目標而選取的：較小的 `n_action_steps` 意味著策略重新觀測與重新規劃的頻率更高（以 30 FPS 計約 0.13 秒），相較於適用於靜態基礎任務物體的 16 步（約 0.53 秒）重規劃間隔更為合適。

### 3.2 訓練流程

#### 3.2.1 超參數

| 訓練執行 | 策略 | 關鍵超參數 | 資料集 |
|---|---|---|---|
| `moving-cube-grasp-fsm-20260607-mike-run5` | Diffusion（預設） | 100,000 步；於 20k/40k/60k/80k/100k 儲存檢查點 | `mikehsuhoodie/moving-cube-grasp-fsm-20260607-mike`（78 個回合 / 48,750 幀） |
| `moving-cube-grasp-act-0609` | ACT | `chunk_size=16`，`n_action_steps=16`；100,000 步 | `yun0523/moving-cube-grasp`（195 個回合） |
| `moving-cube-temporal-es-v1` | SmolVLA + 時序（4 幀 × 2 相機） | `chunk_size=16`，`n_action_steps=4`，`MAX_STEPS=200000`，`EVAL_EVERY=20000`，早停條件 `PATIENCE=3`，`MIN_DELTA=1e-4`，`VAL_RATIO=0.1`，`SPLIT_SEED=42`，`video_backend=pyav` | `yun0523/moving-cube-grasp`，切分為 175 訓練 / 20 驗證回合（每個訓練回合 625 個訓練樣本） |

#### 3.2.2 訓練配置

- **Diffusion（第一階段）**：在雲端 GPU 上以 `lerobot-train --policy.type=diffusion` 訓練，日誌記錄至 Weights & Biases（[訓練連結](https://wandb.ai/seashuwei-national-yang-ming-chiao-tung-university/lerobot/runs/364ncuya)）。100k 步訓練約耗時 4 小時。
- **ACT（第二階段）**：在共用實驗室主機（tmux session）以 `lerobot-train --policy.type=act --policy.chunk_size=16 --policy.n_action_steps=16` 訓練，共 100k 步。
- **SmolVLA 時序（第二階段）**：透過 `cmd/train_moving_cube_smolvla_temporal_early_stop.sh` 訓練，每 20k 步進行一次驗證，並根據驗證損失進行早停。截至本報告撰寫時，**20k 與 40k 步檢查點已完成訓練並通過評估；60k 步檢查點仍在訓練中**，早停尚未觸發。

---

## 4. 實驗結果

### 4.1 資料品質分析

兩次 FSM 資料生成執行均達到 **97.5% 成功率**（第一階段 78/80，第二階段 195/200），表明特權 FSM 專家在兩種運動模型下均能可靠地解決任務。然而，原始成功率本身無法反映*多樣性*：第一階段的 80 個生成位置全部集中在工作空間中心附近，且方塊速度方向偏向「+y，遠離機械臂方向」（方位角 48.5°–143.4°，平均值 91.8°），因此 78 個成功示範在（位置，方向）上高度相關。第二階段的均勻 200 個位置池搭配方向與位置完全解耦的設計，正是專為修正此問題而設，代價是拒絕採樣的問題稍微困難了一些（靠近置物籃或桌面邊緣的方塊會被排除）。

### 4.2 策略模型的量化與質化結果

#### 4.2.1 訓練損失曲線

*（佔位圖——請在此插入來自 §3.2.2 連結之 W&B 執行的訓練/驗證損失曲線。）*

我們注意到第一階段 Diffusion 執行達到了**非常低的訓練損失**，結合資料集有限的多樣性（§4.1），這可能是記憶特定示範而非學到泛化追蹤行為的跡象（詳見 §5 討論）。

#### 4.2.2 Rollout 成功率

**第一階段 — Diffusion Policy（`moving-cube-grasp-fsm-20260607-mike-run5`，100k 步），方塊速度 0.12 m/s：**

| 動作時域 | 回合數 | 捕捉率 | 放置率（給定已捕捉） | 端對端成功率 |
|---|---|---|---|---|
| 1 | 5 | 40%（2/5） | 100%（2/2） | 40%（2/5） |
| 4 | 20 | **65%（13/20）** | **100%（13/13）** | **65%（13/20）** |

`action_horizon=4` 配置在 0.12 m/s 速度下**達成了我們的兩項專題目標**（捕捉率 ≥ 60%，放置率 ≥ 80%），也是整個專題中取得的最佳結果。主要失敗模式集中在**抓取步驟**——一旦方塊被捕捉，放入置物籃的成功率幾乎是 100%。

**第二階段 — ACT（`moving-cube-grasp-act-0609`，100k 步，chunk 16 / `action_horizon=4`），20 回合，20 秒每回合：**

| 捕捉/端對端成功率 |
|---|
| ≈ 5%（1/20） |

觀察到的失敗模式：生成位置過於靠近機器人基座的方塊無論策略品質如何均無法被抓取；靠著置物籃壁滑動的方塊無法被乾淨地夾起；機械臂有時在尚未真正抓取到方塊的情況下便向置物籃移動（好像要放開一樣）。

**第二階段 — SmolVLA + 時序輸入（`moving-cube-temporal-es-v1`，20k 與 40k 步檢查點）：**

在 20k 與 40k 步檢查點的 rollout 中均未觀察到任何端對端成功捕捉。機械臂呈現出明顯朝方塊追蹤的行為，但攔截時機不穩定——在部分回合中機械臂超前於方塊（超調），另一些回合中則落後而錯失抓取時機。60k 步檢查點在撰稿時仍在訓練中，尚未進行評估。

#### 4.2.3 Rollout 截圖

*（佔位圖——請在此插入第一階段 Diffusion 成功案例與第二階段 ACT/SmolVLA 失敗案例的 rollout 截圖/影格。）*

### 4.3（進階）評估流程

#### 4.3.1 評估標準與環境配置

我們設計了 `eval/moving_cube_grasp_eval.py`，掃描三個速度等級——**0.08 m/s**（慢速，低於訓練範圍，屬於分布外）、**0.12 m/s**（中速，在訓練範圍 `[0.10, 0.18]` 內）、**0.18 m/s**（快速，在訓練範圍上緣）——透過 `scripts/rollout.py` 在每個速度下執行可配置次數的 rollout，並解析捕捉/放置/端對端成功率。成功的判定嚴格依賴環境的 `success` 終止項（這是在發現早期 `rollout.py` 將*任何*終止——包括方塊滑落桌面——都計為「成功」後引入的修正）。

#### 4.3.2 評估設計的理由

三速度掃描的設計目的是雙向測試泛化能力：0.08 m/s 測試策略是否能應對比訓練更慢的目標（此時視覺速度線索更弱），而 0.18 m/s 測試訓練分布上緣的情況（方塊留給機械臂的攔截時間最少）。將「成功」限定為明確的 `success` 終止項（而非任何回合結束條件）是必要的，因為早期指標定義會悄悄地將「方塊滑落桌面」與「任務完成」混為一談。

### 4.4（進階）結果

完整的三速度掃描（`eval/moving_cube_grasp_eval.py`）**未能按原計劃執行完成**——時間與算力限制導致我們僅進行了臨時的單速度 rollout（全部在 0.12 m/s，結果已在 §4.2.2 中報告），而非針對每個檢查點進行完整的 0.08/0.12/0.18 m/s 自動化掃描。0.12 m/s 的 Diffusion 結果（`action_horizon=4` 達到 65% 捕捉率、100% 放置率、65% 端對端成功率）因此是我們最佳的已驗證數據點，跨完整速度範圍的泛化測試在提交時仍未完成（見 §5.3）。

---

## 5. 討論

### 5.1 洞察與觀察

- **我們的第一階段自行生成資料集與 Diffusion Policy 是整個專題中效果最好的配置。** 在修正一系列 rollout 基礎設施錯誤（§5.2）後，以 78 個合成示範訓練的 Diffusion 檢查點在 0.12 m/s 下達到了 65% 捕捉率 / 100% 放置率 / 65% 端對端成功率，滿足了兩項專題目標。
- **`action_horizon` 對*固定*檢查點有很大影響，且無需重新訓練**：從 `action_horizon=1` 增加到 4 時，捕捉率大致翻倍（40% → 65%）。對於動態目標，更頻繁地重規劃（`action_horizon=1`）並非自然地更好——似乎每步額外的 DDIM 去噪計算引入了抖動，對追蹤的負面影響大於理論上更快的反應速度帶來的好處。
- **將運動模型泛化（將方塊方向與生成位置解耦、加入預測性前導項）並未將第一階段的成功遷移到新架構。** ACT（5%）和 SmolVLA-時序（目前 0%）在更多樣的第二階段資料集上的表現，遠遜於 Diffusion Policy 在較窄的第一階段資料集上的結果。這表明第一階段的結果雖然達到了我們的數值目標，但可能更多地依賴資料集在（位置，方向）上的狹窄相關性，而非真正的攔截能力——這個假說在截止日前我們未能透過消融實驗充分驗證。
- 一旦方塊被捕捉，**放入置物籃基本上已是解決的問題**（在我們測試的每種配置中均為 100%）——所有剩餘的難度都集中在攔截/抓取步驟。

### 5.2 遭遇的挑戰

- **Rollout 基礎設施 Bug。** 早期 rollout 嘗試（jitter=0 測試，§0605）揭示了五個獨立問題：(1) rollout 中方塊完全不動，因為 `ManagerBasedRLEnv.step()` 沒有 `_pre_physics_step` 鉤子供速度注入程式碼附掛；(2) 方塊始終向同一方向滑動，因為其生成位置與工作空間中心完全重合，導致方位角計算退化為零；(3) `z_range` 成功條件過寬，以至於機械臂夾持方塊懸停在置物籃*上方*就已算成功；(4) 滑落桌面的方塊沒有終止條件，會一直等到超時；(5) rollout 的方塊方向分布與訓練分布不一致。在任何有意義的評估數字可被信任之前，這五個問題均透過六次連續提交逐一修復。
- **資料生成與 Rollout 動力學不一致。** FSM 原本在夾爪閉合後（階段 2）停止注入方塊速度，但 rollout 環境持續注入速度直到方塊被舉起——這意味著*示範*與*測試時的動力學*存在分歧。我們將兩者對齊為「持續注入速度直到方塊被舉起」，並在修正後的動力學下重新生成了第一階段資料集（78 個回合）。
- **算力與工具鏈限制。** 數次雲端 GPU session 因定價/可用性問題而中斷，某供應商 L40S 實例上反覆出現的 VirtualGL 配置問題使 rollout 視覺確認無法進行（數值日誌仍可產出，但無法進行視覺交叉驗證）。
- **截止日前的時間壓力。** 第二階段泛化路線（200 個位置的資料集、方向解耦的運動模型、前導項、ACT 再到 SmolVLA）是在提交截止前約一週才啟動的，這不足以完成 SmolVLA 的完整訓練與評估（目前達到 60k/200k 步），也無法執行完整的三速度評估掃描。

### 5.3 未來方向

- 完成 SmolVLA-時序訓練（目標 200k 步並搭配早停），並在 0.12 m/s 下評估結果，以與第一階段 Diffusion 進行公平比較。
- 在最佳檢查點上執行完整的 `eval/moving_cube_grasp_eval.py` 三速度掃描（0.08/0.12/0.18 m/s），實際測量泛化能力，如同原始計劃所述。
- 在資料生成（失敗的攔截嘗試不再直接丟棄，以降低選擇偏差）和 rollout（抓取失敗後立即重置，而非讓機械臂空手無益地移向置物籃）中加入**重試機制**。
- 增加資料集中「抓取」幀的比例（目前約佔 6%，為 FSM 第 2 階段），可透過延長抓取階段或在訓練時進行加權採樣，讓策略看到更多正確抓取時機的示範。
- 為策略提供更明確的運動線索——例如增加 Diffusion 系策略的 `n_obs_steps`，或將相機估算的輕量方塊位置估計加入輔助觀測——因為在 0.12 m/s 與 60 Hz 的條件下，方塊每幀僅移動約 2 mm，從原始像素差異推斷速度極為困難。
- 在完整速度範圍內訓練（甚至加入非固定運動模型——曲線路徑、週期性加速），直接以泛化能力為訓練目標，而非事後評估。
- 進行相機消融實驗（僅腕部 vs. 僅正面 vs. 兩者皆用），量化各視角對追蹤移動方塊的貢獻。

---

## 6. 工作分配

*（待填入。）*

---

## 參考資料

- LeRobot: https://huggingface.co/lerobot
- Isaac Sim: https://docs.isaacsim.omniverse.nvidia.com/5.1.0/index.html
- Diffusion Policy: https://diffusion-policy.cs.columbia.edu/
- 第一階段資料集: https://huggingface.co/datasets/mikehsuhoodie/moving-cube-grasp-fsm-20260607-mike
- 第一階段訓練執行（W&B）: https://wandb.ai/seashuwei-national-yang-ming-chiao-tung-university/lerobot/runs/364ncuya
- 第二階段資料集: `yun0523/moving-cube-grasp`
- 第二階段 ACT 檢查點: `ann0000000/moving-cube-grasp-act-0609`
